Allocate on GPU (cudaMalloc)
Copy data to GPU (cudaMemcpy Host→Device)
Launch kernel (<<<blocks, threads>>>)
Copy results back (cudaMemcpy Device→Host)
Free GPU memory (cudaFree)

Rule: Always (destination, source, size, direction)

Amdahl's law = 1 / fraction of sequential portion  + (fraction of parallelized portion/speed up)
